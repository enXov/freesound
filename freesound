#!/bin/bash
# Freesound CLI wrapper
# Source the virtual environment and run the tool
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/venv/bin/activate"
python "$SCRIPT_DIR/freesound.py" "$@"
